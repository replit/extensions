{
  "name": "@replit/extensions",
  "version": "0.36.0",
  "description": "",
  "exports": {
    "./package.json": "./package.json",
    ".": {
      "types": "./dist/index.d.ts",
      "import": {
        "default": "./dist/index.js"
      },
      "default": "./dist/index.cjs",
      "require": "./dist/index.js"
    }
  },
  "types": "./dist/index.d.ts",
  "type": "module",
  "unpkg": "dist/index.global.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && tsup src/index.ts  --sourcemap --dts --format esm,cjs,iife --global-name replit",
    "lint": "npx prettier --write src/* && cd example && npx prettier --write .",
    "lint:check": "npx prettier -l src/* && cd example && npx prettier -l .",
    "type:check": "tsc --noEmit",
    "dev": "npm run build && cd ./example && npm install && npx vite",
    "prepublishOnly": "npm run build",
    "generate:docs": "npx typedoc src/index.ts --json docs/main.json --plugin ./util/signature-plugin/index.cjs"
  },
  "files": [
    "dist/*"
  ],
  "keywords": ["replit", "extensions", "api-client"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@codemirror/state": "^6.2.0",
    "comlink": "^4.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.15.18",
    "prettier": "^2.7.1",
    "tsup": "^6.6.3",
    "typedoc": "^0.23.21",
    "typescript": "^4.9.3"
  }
}
