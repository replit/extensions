{
  "name": "@replit/extensions",
  "version": "0.3.0",
  "description": "",
  "main": "build/index.js",
  "module": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx esbuild --bundle src/index.js --outfile=build/index.js  --platform=browser --sourcemap --format=iife --global-name=replit && cp build/index.js example/replit.js && cp build/index.js.map example/replit.js.map",
    "lint": "npx prettier --write src/*",
    "dev": "npm run build && npx vite ./example"
  },
  "files": [
    "index.js",
    "build/*"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "comlink": "^4.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^2.0.1",
    "esbuild": "^0.15.12",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "vite": "^3.0.8"
  }
}
